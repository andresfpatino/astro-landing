---
import {projects} from "../../data/projects.json";
---

<hr>

<h1 class="text-center font-bold text-5xl mt-10">Projects</h1>

{
    projects.map(project => (
        <div>
            <h2>{project.title}</h2>
            <p>{project.description}</p>
        </div>
    ))
}

<hr>